# Razorpay public key (exposed to browser via import.meta.env)
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id

# Backend URL (leave empty in dev — Vite proxy handles it automatically)
VITE_API_URL=https://your-backend.vercel.app
